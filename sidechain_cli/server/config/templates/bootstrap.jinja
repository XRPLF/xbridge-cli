{
  "mainchain_door": {
    "id": "{{ mainchain_id }}",
    "seed": "{{ mainchain_seed }}"
  },
  "sidechain_door": {
    "id": "{{ sidechain_id }}",
    "seed": "{{ sidechain_seed }}"
  },
  "witness_reward_accounts": {{ witness_reward_accounts }}
}
